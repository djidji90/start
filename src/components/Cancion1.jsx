import { Balcony } from '@mui/icons-material';
import { Card, Box, Button, TextField, Typography, Alert, CircularProgress, ButtonBase, colors } from '@mui/material';
import { blueGrey } from '@mui/material/colors';

export default function Canción1(){
    
        return( <Button variant='outlined' sx={{ml: 1}} >
                <Typography color='info' fontFamily={'cursive'}  >
hola bienvenido a esta seccion tan especial.
desde aqui puedes compartir tu musica con el resto del mundo, has que el resto del mundo descubra el talento que se esconde en tu interior.
</Typography>
        </Button>
        ); 
}